import { CoreDiDirective } from './core-di.directive';

describe('CoreDiDirective', () => {
  it('should create an instance', () => {
    const directive = new CoreDiDirective();
    expect(directive).toBeTruthy();
  });
});
